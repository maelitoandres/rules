rules: []
# ══════════════════════════════════════════════════════════════
# 底部规则文件（插入 rules 尾部、MATCH 之前）当前【为空】。
# ══════════════════════════════════════════════════════════════
# 原设备 2 的全流量兜底 SRC-IP-CIDR,@@OPS_IP2@@ 已于 2026-08-20 移到顶部文件
# openclash_custom_rules.list（与设备 1 一起，见那里的「整机 fail-closed 兜底」区）。
#
# 为什么移走：底部位置排在 rules 中部的 RULE-SET,ChinaIp 之后，裸 IP 直连（国内 IP）
# 在 ChinaIp 处就被 DIRECT 截走，底部兜底根本够不着 —— 等于形同虚设，且一直在漏
# 真实 IP（TTNet/HTTPDNS 裸 IP 通道）。fail-closed 兜底必须放顶部、抢在所有 DIRECT
# 类规则之前。2026-08-20 实测确认设备 2 的底部兜底对裸 IP 无效。
#
# 保留本文件（空 rules）是因为 yml_rules_change.sh 硬编码加载它；将来若需要"仅对某
# 设备、且必须排在国内直连之后"的底部规则，再往这里加。空列表不会影响顶部文件解析。
