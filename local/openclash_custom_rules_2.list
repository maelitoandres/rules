rules:
# ══════════════════════════════════════════════════════════════
# 兜底规则（插入位置：rules 的【底部】）
# ══════════════════════════════════════════════════════════════
# OpenClash 把本文件插在 rules 尾部，锚点优先级为
#   rindex(DST-PORT,80) → rindex(MATCH) → rindex(GEOIP)
# 本配置无 DST-PORT,80，故锚点是 MATCH，规则落在
#   ... → GEOIP,CN,🎯 全球直连 → 【本文件的规则】 → MATCH,🐟 漏网之鱼
#
# 也就是说：国内域名/IP 仍走直连不受影响，只有真正未匹配任何规则的
# 流量才会命中这里 —— 等价于把「🐟 漏网之鱼」按源 IP 分流。
# 这是实现「漏网之鱼绑定某设备」的唯一正确位置，写在顶部文件里会
# 连国内流量一起截走。

#—— 运营设备 2：漏网之鱼走专属出口 ——
# ⚠️ 这条会让 @@OPS_IP2@@ 的【所有未匹配流量】从运营 IP 出去：
#    广告 SDK、遥测上报、软件更新检查、各类 app 心跳、证书吊销查询……
#
#    仅适用于【专机专用】的运营设备。好处是流量完全同源，不会出现
#    「业务从哥伦比亚、遥测从中国」这种风控最敏感的矛盾信号；
#    代价是这些杂项流量会消耗代理流量，并计入该 IP 的行为画像。
#
#    若这台设备还兼作日常使用，删掉这一条 —— 上面顶部文件里的 app
#    规则已经覆盖了社媒流量，杂项走本地直连即可。
- SRC-IP-CIDR,@@OPS_IP2@@/32,🇨🇴 运营02

# 注：设备 1（@@OPS_IP1@@）刻意【不加】这条。它只把社媒 app 导向运营出口，
# 其余流量走本地，兼顾日常使用。两台设备的策略差异是有意为之。
